<nav class="fixed inset-x-0 top-0 z-50  backdrop-blur-sm">
    <div class="border-b border-accent bg-background/80">
        <div class="container flex flex-wrap items-center justify-between mx-auto p-4">
            <a href="{{ path('home') }}" class="flex items-center gap-2">
                <img src="https://flowbite.com/docs/images/logo.svg" class="h-8" alt="Flowbite Logo"/>
                <span class="text-2xl font-semibold truncate">Simon Honoré</span>
            </a>
            <button data-collapse-toggle="navbar-default" type="button"
                    class="inline-flex items-center p-2 w-10 h-10 justify-center text-sm text-foreground/70 rounded-lg lg:hidden focus:bg-accent focus:outline-none focus:ring-2 focus:ring-ring"
                    aria-controls="navbar-default" aria-expanded="false">
                <span class="sr-only">Ouvrir le menu</span>
                <svg class="w-5 h-5" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" fill="none"
                     viewBox="0 0 17 14">
                    <path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                          d="M1 1h15M1 7h15M1 13h15"></path>
                </svg>
            </button>
            <div class="hidden w-full lg:block lg:w-auto" id="navbar-default">
                <ul class="font-medium flex flex-col p-4 lg:p-0 mt-4 border border-border rounded-lg bg-card lg:flex-row lg:gap-8 lg:mt-0 lg:border-0 lg:bg-transparent">
                    {% for link, href in links %}
                        <li>
                            <a href="{{ href }}"
                               class="block py-2 px-3 rounded hover:text-primary lg:border-0 lg:p-0"
                            >{{ link }}</a>
                        </li>
                    {% endfor %}

                </ul>
            </div>
        </div>
    </div>
</nav>