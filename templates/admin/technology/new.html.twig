{% extends 'admin/adminBase.html.twig' %}

{% block title %}Nouvelle techno{% endblock %}

{% block body %}

    <div class="px-4 py-12 max-w-3xl m-auto">
        <h1 class="text-2xl font bold mb-8">Nouvelle techno</h1>

        {{ form_start(form) }}
        {{ form_row(form.name, {label: 'Nom de la techno'}) }}
        {{ form_row(form.SymfonyUxIconName, {label: 'Icône Symfony UX'}) }}
        {{ form_rest(form) }}


        <button type="submit" class="btn btn-primary">Enregister</button>

        {{ form_end(form) }}
    </div>

{% endblock %}
